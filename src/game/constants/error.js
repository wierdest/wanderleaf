export const FAILED_TO_FETCH_MANIFEST = (url) => `Failed to fetch manifest.json from ${url}`
export const NOT_IMPLEMENTED = (someClass, someMethod) => `${someClass} must implement ${someMethod}`
export const INVALID_ARGUMENT = (someClass, someArgument) => `${someClass}: Invalid or missing argument '${someArgument}'`
